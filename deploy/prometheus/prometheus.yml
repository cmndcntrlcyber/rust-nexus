# v1.3 Prometheus scrape config for docker-compose (Phase 1.3.9 — scaffold).

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: nexus-server
    static_configs:
      - targets: ['nexus-server:9100']
