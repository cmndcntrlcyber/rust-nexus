// nexus-mesh — libp2p mesh transport for rust-nexus (v2.9.6).
//
// Phase 0 scaffold: empty crate that compiles. Concrete behavior arrives in
// Phase 1+ (identity, libp2p node, Kademlia, Gossipsub, sealed envelopes,
// store-and-forward DTN). All new code lives behind the workspace `mesh`
// feature on consuming crates and the `[mesh].enabled` config flag at runtime.
