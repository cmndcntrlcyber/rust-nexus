# v1.5.3 — Test Coverage: C+ → A

**Priority:** P1
**Baseline grade:** C+
**Target grade:** A
**Depends on:** v1.5.1 Stage 1.1 (build fix)
**Status:** COMPLETE

**A threshold:** Every crate has >= 60% file coverage (files with
`#[cfg(test)]` modules). Integration tests compile and pass. nexus-console
has at least smoke tests. Total test count >= 200.

**Result:** 269 tests passing (was 169). 0 failures. nexus-console has 12
smoke tests across 2 files (40% file coverage).

---

## Stage 3.1 — Unblock nexus-agent and integration tests

Unblocked by v1.5.1 Stage 1.1. nexus-agent now passes 37 tests (13 unit +
24 async). Integration tests compile.

---

## Stage 3.2 — nexus-web-comms tests (43% → 71%)

Added test modules to 2 new files (5 of 7 files now have tests):

- `nexus-web-comms/src/http_fallback.rs` — 4 tests: construction,
  stub exfiltration returns OK, server construction, server start
  returns error stub
- `nexus-web-comms/src/websocket_fallback.rs` — 2 tests: construction,
  Default trait

**Result:** 18 tests passing (was 4).

---

## Stage 3.3 — nexus-webui tests (20% → 60%)

Added test modules to 2 new files (3 of 5 files now have tests):

- `nexus-webui/src/handlers.rs` — 3 tests: TaskExecutionResponse
  serialization, TaskExecutionRequest deserialization, domain list
  JSON shape
- `nexus-webui/src/templates.rs` — 3 tests: construction, Default,
  render returns placeholder

**Result:** 8 tests passing (was 2).

---

## Stage 3.4 — nexus-recon tests (20% → 60%)

Added test modules to 2 new files (3 of 5 files now have tests):

- `nexus-recon/src/browser_fingerprint.rs` — 3 tests: construction,
  Default, stub fingerprint fields
- `nexus-recon/src/network_recon.rs` — 2 tests: construction, stub
  scan result fields

**Result:** 8 tests passing (was 3).

---

## Stage 3.5 — nexus-hybrid-exec tests (20% → 60%)

Added test modules to 2 new files (3 of 5 files now have tests):

- `nexus-hybrid-exec/src/ssh_executor.rs` — 2 tests: construction,
  execute without `ssh` feature returns error
- `nexus-hybrid-exec/src/api_executor.rs` — 2 tests: construction,
  execute without `api` feature returns error

**Result:** 11 tests passing (was 5).

---

## Stage 3.6 — nexus-console smoke tests (0% → 40%)

Added test modules to 2 files (2 of 5 files now have tests):

- `nexus-console/src-tauri/src/state.rs` — 6 tests: construction,
  Default, session ID increment, no-connection error, summary is None
  when disconnected, active count starts at zero
- `nexus-console/src-tauri/src/commands.rs` — 6 tests: hex_lower
  empty/known values, audit chain verify empty/valid/tampered,
  AuditFilter default

**Result:** 12 tests passing (was 0).
